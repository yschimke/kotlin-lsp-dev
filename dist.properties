# Pinned kotlin-lsp release the compile-check resolves against.
#
# This is the newest build that exists: the CDN 404s on every later build number and the VS
# Code Marketplace agrees (extension 0.0.5 bundles exactly LS-262.8190.0). Upstream master
# therefore runs ahead of it -- see "Version skew" in README.md.
#
# Update with `scripts/fetch-dist.sh --check` to probe for something newer.
kotlinLspVersion=262.9593.0
