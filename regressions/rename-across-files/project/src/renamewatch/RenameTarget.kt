package renamewatch

// Renamed by the regression check. Referenced from Consumer.kt, which is what makes the check
// able to tell a complete rename from a partial one.
class RenameTarget {
    fun label(): String = "target"
}
